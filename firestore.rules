rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // TODO : 일단은 테스트용으로 읽기,쓰기 권한 모두 true로 변경해둠. 나중에 수정
    }
  }
}